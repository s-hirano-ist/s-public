---
import { markdownToToc } from "@utils/markdownToHtml";
import sanitizeHtml from "sanitize-html";

export type Props = {
  post: string;
};

const { post } = Astro.props;

const content = await markdownToToc(post);
---

<Fragment set:html={sanitizeHtml(content)} />
