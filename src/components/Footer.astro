---
import Hr from "@components/Hr.astro";

const currentYear = new Date().getFullYear();

export type Props = {
  darkModeDisabled?: boolean;
};
const { darkModeDisabled = false } = Astro.props;
---

<footer
  class={`w-full ${
    darkModeDisabled ? "text-foreground" : "text-muted-foreground"
  } mx-auto max-w-5xl px-4`}>
  <Hr noPadding darkModeDisabled={darkModeDisabled} />
  <div class="flex justify-between py-4">
    <div class="my-2 flex items-center whitespace-nowrap">
      <span>Copyright &#169; {currentYear}</span>
      <span>&nbsp;|&nbsp;</span>
      <span>All rights reserved.</span>
    </div>
    <a
      href="/license"
      class="my-2 text-sm underline underline-offset-4 transition-opacity hover:opacity-70">
      License
    </a>
  </div>
</footer>
